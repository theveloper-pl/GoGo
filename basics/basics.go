package main

import (
	"fmt"
)

func main() {
	fmt.Println("Hello world !")


	// cards := []string{newCard(),newCard()}
	// fmt.Printf("%#v \n", cards)
	// cards = append(cards,newCard())
	// fmt.Printf("%#v \n", cards)

	// for i, card:= range cards{
	// 	fmt.Printf("%#v %d\n", card, i)
	
}